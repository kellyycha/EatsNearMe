package com.example.eatsnearme.profile

import androidx.lifecycle.ViewModel

class ProfileViewModel : ViewModel() {

    fun changeUsername(){
        // change username in parse
    }

    fun changePassword(){
        // type current and new password. check if current password matches
    }

    fun changeEmail(){
        // check if valid email address
    }
}